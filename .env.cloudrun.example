# Non-secret Cloud Run runtime settings
NODE_ENV=production
AUTH_SESSION_TTL_SECONDS=43200

# Inject these from Secret Manager; never put production values in this file.
DATABASE_URL=secret:database-url-runtime
AUTH_SECRET=secret:auth-secret
OPENAI_API_KEY=secret:openai-api-key
